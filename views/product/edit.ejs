<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title></title>
</head>
<body>
<!--The content below is only a placeholder and can be replaced.-->

<%- include('../header') %>


<div class="container-fluid">
    <div class="row">
       <%- include('../menu') %>

        <div class="col-sm-10">
            <ol class="breadcrumb">
                <li class="active">商品管理
                </li>
                <li class="active">编辑商品
                </li>
            </ol>
            <div class="panel panel-default">
              
			    <div class="panel-heading">
                  编辑商品
                </div>
                <div class="panel-body">
                    <div class="table-responsive input-form">
                        <form action="/product/edit" method="post" enctype="multipart/form-data">
                            <ul>
                                <li>  商品名称: <input type="text" name="name" value="<%= result.name %>"/></li>

                                <li>  商品价格: <input type="text" name="price" value="<%= result.price %>"/></li>
                                <li>  商品详情: 
                                    
                                    <textarea name="desc" id="" cols="30" rows="10">
                                            <%= result.desc %>
                                    </textarea>
                                </li>
                                <li><%= result.typeid %></li>
                               
                                <li>
                                    商品类别：
                                        <select name="typeid">
                                            <% for(var i=0;i<typeResult.length;i++) {%>
                                            <option 
                                                value="<%= typeResult[i]._id %>"
                                                <%= result.typeid.toString() == typeResult[i]._id.toString()? 'selected' : ''  %>
                                            ><%= typeResult[i].name %></option>
                                            <% } %>
                                        </select>
                                </li>

                                <li>  商品图片: <input type="file" name="picname"/></li>
                                <input type="hidden" name="oldpicname" value="<%= result.picname %>" />
                                <input type="hidden" name="_id" value="<%= result._id %>" />

                                <li>
                                    <br/>
                                    <button type="submit" class="btn btn-default">编辑</button>
                                </li>

                            </ul>


                        </form>
                    </div>
                </div>


            </div>

        </div>
    </div>
</div>

</body>
</html>