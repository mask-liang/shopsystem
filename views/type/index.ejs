<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title></title>
</head>
<body>
<!--The content below is only a placeholder and can be replaced.-->


<!-- header -->
<%- include('../header') %>

<div class="container-fluid">
    <div class="row">
       

        <!-- menu -->
        <%- include('../menu') %>
        <div class="col-sm-10">
            <ol class="breadcrumb">
                <li class="active">类别管理
                </li>
                <li class="active">类别列表
                </li>
            </ol>
            <!--
                列表展示
            -->
            <div class="table-responsive">
                <table class="table table-striped ">
                    <thead>
                    <tr>
                        <th>ID</th>
                        <th>类别名称</th>
                        <th>类别图片</th>
                       
                        <th class="text-center">操作</th>
                    </tr>
                    </thead>
                    <tbody>
                    <% for(var i=0;i<typeResult.length;i++){ %>
                    <tr>
                        <td><%= typeResult[i]._id %></td>
                        <td><%= typeResult[i].name %></td>
                        <td>
                            
                            <img src="/uploads/<%= typeResult[i].picname %>" width="50" height="50" alt="">
                        </td>
                      
                        <td class="text-center"><a href="/type/edit?_id=<%= typeResult[i]._id %>">修改</a>  <a href="/type/delete?_id=<%= typeResult[i]._id %>&picname=<%= typeResult[i].picname %>">删除</a></td>
                    </tr>
                    <% } %>

                    </tbody>
                </table>
            </div>

        </div>
    </div>
</div>

</body>
</html>